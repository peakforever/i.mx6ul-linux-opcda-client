# 项目状态记录(2026-08-25 收尾)

接续入口:说"继续 OPC DA client 工作"即可。关联:docs/STATUS-2026-08-20.md(旧)。

## 一、OPC DA client(Java 线)

当前分支:feat/precheck-points(最新 dd99f82,已推远端)
本地路径:~/i.mx6ul-linux-opcda-client(构建:mvn clean package,124 测试全绿)

### 今日完成(2026-08-25)

- AGENTS.md 项目上下文(3ae5132)
- 包名改名 com.taiji.opc2ecu → com.opc2ecu(3b9401e,37 文件纯机械替换)
- 归因面实现:DiagCode 枚举(HRESULT/Win32 双编码映射)+ [DIAG] 输出行,
  connect()/listServers() 失败归因(feed54e,123 测试)
- ReconnectManager 归因透传:带 DiagCode 的连接异常不再包装成含
  "检查防火墙"的旧消息(4db541a,124 测试)
- verify-opcda.sh:BusyBox 兼容(timeout→run_with_timeout、head/ps 语法)
  + 子 shell 隔离修复(BusyBox ash 1.31.0 前缀赋值+函数调用变量泄露,
  设备实测 TEST_VAR=x;本机 1.37.0 已修复,版本差异)+ 入库
  scripts/verify-opcda.sh(61c1916)
- 部署包重建:target/opc2ecu-imx6ul-armhf.tar.gz(新 jar + Zulu compact1 JRE,
  SHA-256 48896cc8... 校验通过)
- 操作手册:docs/build-package-deploy-manual.md(八章:构建/打包/转包/部署/
  验收/排查/速记/新 Server 发现流程,含 ssh-rsa 与 scp -O 兼容说明)
- 实机验证:ECU 生产链路打通(precheck 连接成功,10/12 点位可读;
  服务正常采集 12 item → 127.0.0.1:5353);verify 修复版待最终确认

### 待办(明天)

1. ECU 换新 jar(ReconnectManager 修复版):
   scp target/opcda-probe.jar → /home/ecu/opc2ecu/lib/(停服务→替换→启动)
2. 连跑两次 verify(修复版,从仓库 scripts/verify-opcda.sh 取),
   确认稳定 RESULT: PASS=8 FAIL=0
3. 数据面 5353 联调(等 C++ 契约 v2.1 确认 + 接收端部署):
   sh verify-opcda.sh --collect
4. 归因面后续(可选):--diagnose 逐层探测模式 / 周期状态行 /
   J-Interop 日志降噪(org.jinterop JUL 压到 WARNING)
5. OPCEnum 组件权限(要用 --list-server 探测时 Windows 侧配 opcuser;
   生产采集直连 Matrikon,不依赖)

### 关键坑与约束(踩坑沉淀)

- ECU BusyBox ash 1.31.0:前缀赋值 `VAR=val 函数调用` 变量泄露不恢复 →
  脚本内一律子 shell 隔离 `( VAR=val; func )`;设备端 shell 行为必须设备实测
- ECU dropbear:scp 需 `-O -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa`
- verify 脚本从仓库取(scripts/verify-opcda.sh),勿用散落拷贝
- points.json 密码明文 600;探测模式密码走 OPC_PASSWORD 环境变量
- 2 个 Sine Waves 点位 not-readable(Matrikon Sine 波形默认关闭,非故障)

## 二、测试工作台(Go 线,未动)

分支 feat/workbench-v1;待办:实机验收(wg/ping/Modbus/内存/web-auth.json),
bin/ecu-web-config 重建方式定案。见 docs/STATUS-2026-08-20.md。

## 三、环境速查

- ECU:192.168.1.234(ecu500),/home/ecu/opc2ecu,BusyBox 1.31.0
- Windows+Matrikon:192.168.1.2,DESKTOP-EB400FS,opcuser
- 本机:189.x 网段到不了现场,需经构建机转包
- JRE:zulu8.76.0.17-ca-cp1-jre8.0.402-linux_aarch32hf(SHA 48896cc8...,
  打包脚本硬编码校验)
