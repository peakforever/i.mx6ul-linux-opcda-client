# 项目状态记录(2026-08-20 收尾)

接续入口:说"继续 OPC DA client 工作"或"继续工作台工作"即可。

## 一、OPC DA client(Java 线)

当前分支:feat/precheck-points(5943c46,已推远端,与 origin 同步)
本地路径:~/i.mx6ul-linux-opcda-client(构建:mvn clean package,111 测试全绿)

### 已完成
- 迭代 1(断线重连/单元测试/异常退出码)— 51 测试
- 迭代 2(多 Item 采集 + 30B UDP 发送 + 心跳 V1.2)— 88 测试
- 迭代 2 收尾(--precheck-points 点表预检 + 周期级看门狗)— 110 测试
- 配置架构 v2.1(server 段入 points.json、md5Charset 默认 UTF-8)— 111 测试
- 审核记录:docs/code-review-iter2-2026-08-20.md、
  docs/code-review-precheck-config-v2-2026-08-20.md
- 契约文档:docs/points-config-format.md v2.1(待发 C++ 同事)

### 契约 v2.1 要点(发给同事前核对)
- points.json 单文件完整配置:server(host/domain/user/password/progId/clsid/
  socketTimeoutMillis/useNtlmV2)+ periodMillis + udp(host/port/md5Charset)
  + reconnect + items
- 密码方案 B:明文入文件,权限 600,禁止入库/日志
- items = OPC Server 实际 Item ID 全字符串 = MD5 输入原文,client 不重组
- 不出现 group 字段(client 采集组名是运行时会话标识,不参与 MD5)
- 文件一律 UTF-8;md5Charset 默认 UTF-8(GBK 仅兼容)

### 联调前置(4 项确认)
1. C++ 接收端(5353 监听+测点表+心跳应答)已部署 ECU?
2. 契约 v2.1 三要点确认:items=MD5 输入原文 / 文件 UTF-8 / 字符集一致
3. 环境:Matrikon 在线、防火墙放行 ECU(192.168.1.234)
4. points.json 密码填真实值(包内是 CHANGE_ME)

### 联调包
~/opcda-deploy/opcda-acceptance-bundle.tar.gz(5.3MB)
部署:scp 到 ecu@192.168.1.234:/tmp → 解包 → cp jar 到
/home/ecu/opc2ecu/lib/、config/ 到 /home/ecu/opc2ecu/config/
执行:export OPC_PASSWORD=***; sh verify-opcda.sh(--reconnect --collect 可选)
注:本机(192.168.189.x)与现场(192.168.1.x)不同网段,需经用户构建机转包

### 分支合入
main 未合入;三条递进分支(iteration-1 → iteration-2 → precheck-points),
联调通过后建议直接合 precheck-points 到 main。

## 二、测试工作台(Go 线)

分支:feat/workbench-v1(cf70e64,已推远端)
本地路径:~/ecu-web-config(go build ./... 通过,现有+27 新测试全绿)

### 已完成
- 工作台 v1 全部功能(隧道状态 wg 只读/网络诊断 ping+端口+扫描/Modbus
  RTU+TCP 只读/前端页面/认证加固)
- 审核记录:docs/code-review-workbench-2026-08-20.md
- 规格:docs/dev-spec-workbench-v1.md

### 待办
- bin/ecu-web-config(ARM32)重建方式未定:Go 1.27 交叉编译(本机 make build)
  vs 用户构建机重出——注意 ECU 的 Go 版本兼容性约束
- 实机验收:wg 真实隧道/ping root 权限/Modbus 485/内存增量/web-auth.json

## 三、部署文档(待编写,下次重点)

- opcda-client 部署文档:目录结构(/home/ecu/opc2ecu/)、JRE(Zulu 8 compact1
  armhf)、systemd 服务单元(opc2ecu.service)、配置(v2.1)、启动/升级/日志
- 工作台部署文档:ecu-web-config.service 已有,补充 workbench 使用说明
- 热重载(watcher)未实现,文档按"重启生效"表述

## 四、环境速查

- ECU:i.MX6UL ecu200,192.168.1.234,Zulu JRE 8 compact1 armhf,
  /home/ecu/opc2ecu(Yocto Zeus,glibc 2.30)
- Windows+Matrikon:192.168.1.2,DESKTOP-EB400FS,opcuser
- 本机:192.168.189.149(与现场不同网段),JDK17+Maven3.9+Go1.27+Codex CLI
- Codex 认证:~/.codex/auth.json 已配置;语法 codex exec --approve-for-me
